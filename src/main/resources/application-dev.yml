spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/youtube_agent
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.youtubeagent: DEBUG
